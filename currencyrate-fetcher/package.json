{
	"name": "currencyrate-fetcher",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "wrangler build",
		"test": "vitest",
		"type-check": "wrangler types && tsc"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.42.0"

	},
	"dependencies": {
		"@types/express": "^5.0.3",
		"express": "^5.1.0"
	}
}
